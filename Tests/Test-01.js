let numbers = [4, 7, 12, 15, 9];
let modifiedNumbers = numbers.map((n) => (n % 2 == 0 ? n * 2 : n + 1));
console.log(modifiedNumbers);

// console.log([4, 7, 12, 15, 9].map((n) => (n % 2 == 0 ? n * 2 : n + 1)));
